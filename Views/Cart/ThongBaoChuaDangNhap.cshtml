@{
    ViewBag.Title = "Thông báo";
}

<div class="container mt-5">
    <div class="alert alert-warning text-center">
        <h4>⚠️ Bạn chưa đăng nhập!</h4>
        <p>Vui lòng <a href="@Url.Action("Login", "Users")" class="btn btn-primary mx-2">Đăng nhập</a> để xem giỏ hàng.</p>
    </div>
</div>
