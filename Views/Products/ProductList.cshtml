@*@model IEnumerable<CNPM_Project_web.Model>
@{
    ViewBag.Title = "Danh sách sản phẩm";

}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<h2 class="text-center">Danh sách sản phẩm</h2>
<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-3 col-sm-6 mb-4" style="border-radius:10px">
            <div class="card shadow-sm" style="height: 100%;">
                <img src="@item.HINH_ANH" class="card-img-top" alt="@item.TEN_SP" style="height: 250px; object-fit: cover;" />
                <div class="card-body d-flex flex-column justify-content-between">
                    <h5 class="card-title" title="@item.TEN_SP">@item.TEN_SP</h5>
                    <p class="card-text text-danger font-weight-bold">
                        @string.Format("{0:#,##0} ₫", item.GIA_BAN)
                    </p>
                    <div class="btn-group mt-auto">
                        @Html.ActionLink("Xem chi tiết sản phảm", "ProductDetails", new { id = item.MA_SP }, new { @class = "btn btn-sm btn-outline-primary" })
                    </div>
                </div>
            </div>
        </div>
    }
</div>*@
