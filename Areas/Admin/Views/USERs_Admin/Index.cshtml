@model IEnumerable<CNPM_Project_web.Model.USER>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PASSWORD)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EMAIL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Khach_Hang.HO_TEN_KH)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role.TEN_ROLE)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PASSWORD)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EMAIL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Khach_Hang.HO_TEN_KH)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role.TEN_ROLE)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.USERNAME }) |
                @Html.ActionLink("Details", "Details", new { id=item.USERNAME }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.USERNAME })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
