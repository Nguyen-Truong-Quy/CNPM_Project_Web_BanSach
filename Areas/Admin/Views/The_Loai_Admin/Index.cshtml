@model IEnumerable<CNPM_Project_web.Model.The_Loai>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten_The_Loai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten_The_Loai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_The_Loai }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_The_Loai }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_The_Loai })
        </td>
    </tr>
}

</table>
